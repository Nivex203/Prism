# Prism
Prism is a code generation engine designed to eliminate manual type duplication. It allows you to use your TypeScript interfaces as the definitive source of truth for your entire stack.  Modify a property in your TypeScript model, and Prism instantly reflects those changes across your Prisma schemas, Zod validators, SQL migrations.
